services:
  - type: web
    name: ai-dashboard
    env: node
    plan: free
    buildCommand: npm run bootstrap && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: JWT_SECRET
        generateValue: true
